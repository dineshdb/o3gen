[package]
name = "o3gen-tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
o3gen = { path = "../o3gen" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
o3gen = { path = "../o3gen" }
